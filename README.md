# 📘 Notion Clone

---

> Простое и современное приложение для заметок с поддержкой обложек, эмодзи-иконок, редактора заголовков и управления страницами.
>
> UI стилизован через **TailwindCSS v4**, логика построена на **React + Context API**.

---

## 🚀 Возможности

```javascript
- Создание новых страниц
- Редактирование названия заметки
- Добавление эмодзи-иконки к заметке
- Установка изображения как обложки (Add Cover)
- Автоматическое сохранение через context
- Удобная панель навигации со списком страниц
- Grid-эмодзи-панель с поиском (EmojiPicker)
```

📌 _Все функции работают без сторонних библиотек для UI — нативно через React и Tailwind._

---

## 📂 Структура проекта

```javascript
/src
├── components
│   ├── EmojiPicker.jsx
│   ├── SearchBar.jsx
│   └── ...
│
├── context
│   └── pagesContext.jsx
│
├── pages
│   └── EditPage.jsx
│
├── App.jsx
└── main.jsx
```

---

## 🎨 Используемые технологии

```javascript
- React 18
- React Router DOM
- TailwindCSS v4
- React Icons
```

---

## 🧠 Основная логика

### 📌 Управление заметками

Хранится в `pagesContext.jsx` и включает:

```javascript
-addPage() - // добавляет новую страницу
  updateNote(id) - // обновляет данные заметки
  findNote(id); // ищет заметку по ID
```

### 📌 Добавление обложки (Cover)

Через скрытый input:

```javascript
<input type="file" accept="image/*" />
```

Картинка отрисовывается так:

```javascript
<img className="w-full h-full object-cover object-top" />
```

---

## 😀 Добавление эмодзи

EmojiPicker появляется рядом с кнопкой и позволяет выбрать эмодзи.

```javascript
- позиция высчитывается через getBoundingClientRect()
- после выбора кнопка заменяется на эмодзи
```

---

## ▶️ Запуск проекта

```javascript
npm install
npm run dev
```

> Приложение поднимется на [http://localhost:5173](http://localhost:5173)

---

## 🧪 Планируемые улучшения

```javascript
- Rich-text редактор (жирный/курсив/заголовки)
- Перетаскиваемые блоки
- Выбор цвета обложки по умолчанию
- Добавление тегов и фильтров
```

---

## ⚠️ Подводные камни

- `object-top` работает только вместе с `object-cover`.
- Tailwind v4 не поддерживает классы типа `h-100` — надо использовать `h-full` или `h-[200px]`.
- Не смешивай `contentEditable` и `value` на одном элементе.

---

## ⭐ Автор

Проект создан М _(и улучшен твоими идеями, бро!)_
